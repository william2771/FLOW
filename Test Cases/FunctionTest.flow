//Function Test
use "SimpleGraph.flowt";

int foo(int x)
{
	int y = 4;
	return x + y;
}

int bar(int y)
{
	return y - 1;
}

int k = 0;
k = foo(k);
print(k);

k = foo(1);
print(k);

k = foo(1 + k);
print(k);

print(foo(bar(k)));

k = bar(k);
print( bar(k));
